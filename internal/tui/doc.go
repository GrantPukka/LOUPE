// Package tui is the full-screen terminal interface behind `loupe tui`.
//
// It is a view over the same query path as the CLI and the web UI, for the
// common case of being on a box with no browser. Like the web UI, it must not
// be able to do anything the CLI cannot.
package tui
